version: 0.2

phases:
  install:
    commands:
      - yarn install
  build:
    commands:
      - yarn sls:remove:$ENV
      - export SLS_DEBUG=false
      - yarn sls:$ENV

artifacts:
  files:
    - serverless.yml
  discard-paths: yes

cache:
  paths:
    - '/node_modules/**/*'
